import Heading from './includes/Heading';
import React from 'react';

const Home = () => {
  return (
    <section className="container">
      <Heading />
    </section>
  );
};

export default Home;
