import Heading from './includes/Heading';
import React, { useEffect } from 'react';

const Home = () => {
  return (
    <section className="container">
      <Heading />
    </section>
  );
};

export default Home;
